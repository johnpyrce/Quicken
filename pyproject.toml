[project]
name = "quicken-tools"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=0.9.0",
    "openpyxl>=3.1.5",
    "playwright>=1.62.0",
    "pyarrow>=14.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-xdist>=3.8.0",
    "ruff>=0.16.3",
]
